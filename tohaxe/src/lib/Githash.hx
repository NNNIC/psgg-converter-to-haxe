package lib;
using StringTools;
import system.*;
import anonymoustypes.*;

class Githash
{
    public static inline var hash:String = "no hash because of debug";
    public function new()
    {
    }
}
